# Example channel prompts (reference only)

These show what an adapter might pass as `append_system_prompt`. The engine does
not load them automatically. Bundled adapters use copies in `adapters/prompts/`.
Video-chat uses `clawd-video-chat/prompts/`.
